import { StateFacilityPipe } from './state-facility.pipe';

describe('StateFacilityPipe', () => {
  it('create an instance', () => {
    const pipe = new StateFacilityPipe();
    expect(pipe).toBeTruthy();
  });
});
